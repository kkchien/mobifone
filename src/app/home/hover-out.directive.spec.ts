import { HoverOutDirective } from './hover-out.directive';

describe('HoverOutDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverOutDirective();
    expect(directive).toBeTruthy();
  });
});
